为 Navidrome 的媒体库扫描增加可审阅的变更预演，使管理员在批量整理文件或启用清理策略前，能从现有管理界面针对全量、快速或指定目录生成一份不改变媒体库可见状态的扫描计划，并按库查看将新增、更新、移动、标记缺失、清理的媒体条目以及播放列表影响和警告；计划必须绑定生成时的库范围、扫描强度、相关配置与文件系统事实，管理员确认后才能应用，应用前若文件、库配置或相关媒体记录已变化则拒绝使用陈旧计划并指出需要重新预演，重复确认不得重复写入；预演和应用都要支持进程内与外部扫描器并报告可区分的阶段进度，计划过期或删除后不可再应用，同时计划功能默认不改变现有 Subsonic 扫描、计划任务、文件监视器、启动扫描和命令行扫描行为。
