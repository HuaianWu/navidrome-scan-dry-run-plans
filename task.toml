schema_version = "1.3"
artifacts = ["/logs/artifacts/model.patch"]
[task]
name = "benzhi/navidrome-scan-dry-run-plans"
description = ""
authors = []
keywords = []
[metadata]
ext_id = "kh0a2a6af818cb3b4247e61bf3ad420d"
task_id = "navidrome-scan-dry-run-plans"
display_title = "Add reviewable dry-run scan plans with protected apply"
display_description = "Generate read-only scan plans per library that list adds, updates, moves, missing marks, and cleanups, then apply them only while their inputs are unchanged."
original_title = "为媒体库扫描增加只读变更预演与受保护应用"
category = "feature_request"
language = "go"
repository_url = "https://github.com/navidrome/navidrome"
base_commit_hash = "afb3a2f881a245c28cc7b65888f51ea1a2cbbb3b"
[verifier]
network_mode = "no-network"
environment_mode = "separate"
timeout_sec = 1800.0

[verifier.env]
[verifier.environment]
build_timeout_sec = 1800.0
cpus = 2
memory_mb = 8192
storage_mb = 20480

[[verifier.collect]]
command = "cd /app && mkdir -p /logs/artifacts && git config --global --add safe.directory /app && git diff --binary afb3a2f881a245c28cc7b65888f51ea1a2cbbb3b HEAD > /logs/artifacts/model.patch"
timeout_sec = 300.0

[agent]
network_mode = "no-network"
timeout_sec = 10800.0
[environment]
build_timeout_sec = 1800.0
# docker_image is omitted in step 1: Harbor/Pier builds environment/Dockerfile.
# After the image is built and pushed, set e.g.
# docker_image = "<registry>/benzhi-swe:kh0a2a6af818cb3b4247e61bf3ad420d-v1"
os = "linux"
cpus = 2
memory_mb = 8192
storage_mb = 20480
gpus = 0
mcp_servers = []

[environment.env]
[solution.env]
